# Carbonio Configuration Variables
pkgver=1.0.0
_buildtype="FOSS"
_major=$(echo "${pkgver}" | cut -d'.' -f1)
_micro=$(echo "${pkgver}" | cut -d'.' -f3)
_minor=$(echo "${pkgver}" | cut -d'.' -f2)
_rc="ZEXTRAS"
_release="KEPLER"
_releaseno="${pkgver}"
